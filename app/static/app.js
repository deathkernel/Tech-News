const feed = document.getElementById('feed');
const status = document.getElementById('status');
const search = document.getElementById('search');
const category = document.getElementById('category');
const importance = document.getElementById('importance');
const refresh = document.getElementById('refresh');
const top10List = document.getElementById('top10List');
const top10Date = document.getElementById('top10Date');
let items = [];

const categoryIcons = { AI:'🤖', LLMs:'🧠', Programming:'💻', 'Cyber Security':'🔐', Cloud:'☁️', Linux:'🐧', Windows:'🪟', Apple:'🍎', Android:'📱', Web:'🌐', Databases:'🗄️', DevOps:'⚙️', 'Open Source':'📦', Research:'🧪', Hardware:'🖥️', 'GPU/CPU':'⚡', 'Developer Tools':'🛠️' };

async function loadCategories(){try{const r=await fetch('/categories');if(!r.ok)throw 0;const d=await r.json();category.innerHTML='<option value="">All topics</option>';for(const name of d.categories){const o=document.createElement('option');o.value=name;o.textContent=name;category.appendChild(o)}}catch{category.innerHTML='<option value="">All topics</option>'}}

async function loadNews(){setLoading(true);status.textContent='Finding high-signal stories + podcasts...';try{const params=new URLSearchParams({minimum_importance:importance.value});const r=await fetch(`/news?${params}`);if(!r.ok)throw 0;items=await r.json();render();renderTop10();loadIntelligence()}catch{status.textContent='Could not load news';feed.innerHTML='<div class="empty">JARVIS could not reach the news sources right now. Try refresh.</div>';top10List.innerHTML='<div class="empty">Daily Top 10 is temporarily unavailable.</div>'}finally{setLoading(false)}}

function renderTop10(){const top=items.slice(0,10);top10Date.textContent=`${new Date().toLocaleDateString([], {month:'short',day:'numeric',year:'numeric'})} · ${top.length} signals`;if(!top.length){top10List.innerHTML='<div class="empty">No high-signal stories found today.</div>';return}top10List.innerHTML=top.map((item,i)=>`<article class="top10-item"><div class="top10-rank">${String(i+1).padStart(2,'0')}</div><div><strong>${escapeHtml(item.title)}</strong><span class="top10-meta">${categoryIcons[item.category]||'⚡'} ${escapeHtml(item.category)} · ${escapeHtml(item.source)} · ${item.importance}/10</span><p>${escapeHtml(cleanText(item.summary).slice(0,180) || 'Technology signal worth tracking.')}</p><a href="${escapeHtml(item.url)}" target="_blank" rel="noopener noreferrer">READ SIGNAL ↗</a></div></article>`).join('')}

async function loadIntelligence(){try{const r=await fetch(`/intelligence?minimum_importance=${importance.value}`);if(!r.ok)return;const d=await r.json();document.getElementById('intelTotal').textContent=d.total??'0';document.getElementById('intelMajor').textContent=d.major??'0';document.getElementById('intelImportant').textContent=d.important??'0';document.getElementById('intelTop').textContent=d.top_stories?.[0]?.category||'NO SIGNAL'}catch{document.getElementById('intelTop').textContent='OFFLINE'}}

function render(){const query=search.value.toLowerCase().trim(),selected=category.value.toLowerCase();const filtered=items.filter(item=>{const searchable=[item.title,item.summary,item.source,item.content_type,...(item.companies||[]),...(item.tags||[])].join(' ').toLowerCase();return(!query||searchable.includes(query))&&(!selected||item.category.toLowerCase()===selected)});status.textContent=`${filtered.length} ${filtered.length===1?'story':'stories'}`;if(!filtered.length){feed.innerHTML='<div class="empty">No stories match these filters.</div>';return}feed.innerHTML=filtered.map((item,index)=>renderPost(item,index)).join('')}

function renderPodcastHighlights(item){if(item.content_type!=='podcast'||!item.highlights?.length)return '';return `<section class="podcast-intel"><div class="podcast-kicker">🎙️ JARVIS PODCAST INTELLIGENCE</div><strong>Most interesting moments</strong>${item.highlights.map((h,i)=>`<div class="podcast-highlight"><span>${i===0?'🔥':'💡'}</span><div><p>${escapeHtml(h.text)}</p><small>${escapeHtml(h.reason||'High-signal technology point')}${h.timestamp?` · ${escapeHtml(h.timestamp)}`:''}</small></div></div>`).join('')}</section>`}

function renderPost(item,index){const summary=cleanText(item.summary)||'A new technology update worth knowing about.',shortSummary=summary.length>270?`${summary.slice(0,267).trim()}...`:summary,companies=item.companies?.length?item.companies.join(' · '):'',icon=categoryIcons[item.category]||'⚡',date=item.published_at?formatDate(item.published_at):'Today',signal=item.importance>=8?'MAJOR UPDATE':item.importance>=6?'IMPORTANT':'TECH UPDATE',imageSource=item.image_url||`/image?url=${encodeURIComponent(item.url)}`,fallback=`<div class="post-image image-fallback" aria-label="${escapeHtml(item.category)} technology visual"><span>${icon}</span><small>${escapeHtml(item.category)}</small></div>`,image=`<img class="post-image" src="${escapeHtml(imageSource)}" alt="${escapeHtml(item.image_alt||item.title)}" loading="lazy" onerror='this.onerror=null;this.parentElement.innerHTML=${JSON.stringify(fallback)}'>`,contentType=item.content_type==='podcast'?'🎙️ PODCAST / INTERVIEW':item.content_type==='github'?'⭐ CURATED GITHUB':signal;return `<article class="post" id="post-${index}"><div class="post-glow"></div><header class="post-header"><div class="post-brand"><span class="post-mark">J</span><div><strong>JARVIS</strong><span>TECH NEWS</span></div></div><span class="post-date">${escapeHtml(date)}</span></header><div class="post-topic"><span class="topic-icon">${icon}</span><span>${escapeHtml(item.category)}</span><i></i><span>${contentType}</span></div><div class="media-frame">${image}</div><h2>${escapeHtml(item.title)}</h2>${renderPodcastHighlights(item)}<section class="story"><span class="label">WHAT HAPPENED</span><p>${escapeHtml(shortSummary)}</p></section><section class="story why-story"><span class="label">WHY IT MATTERS</span><p>${escapeHtml(buildWhy(item))}</p></section><div class="takeaway"><span class="label">KEY TAKEAWAY</span><strong>${escapeHtml(buildTakeaway(item))}</strong></div><div class="post-actions"><button class="post-action" type="button" data-copy-post>COPY POST</button><a class="post-action" href="${escapeHtml(item.url)}" target="_blank" rel="noopener noreferrer">${item.content_type==='podcast'?'WATCH / LISTEN ↗':'OPEN SOURCE ↗'}</a></div><footer class="post-footer"><div><span class="source">${escapeHtml(item.source)}</span>${companies?`<span class="companies">${escapeHtml(companies)}</span>`:''}</div><span class="handle">@JARVIS</span></footer></article>`}

function buildWhy(item){const category=item.category,companies=item.companies?.length?item.companies.join(', '):'the technology ecosystem';if(item.content_type==='podcast')return'This conversation can surface ideas, plans and technical insights that may not appear in a standard news headline. JARVIS highlighted the strongest available technology points.';if(item.content_type==='github')return'This repository comes from a curated high-signal technology creator or organization. JARVIS only surfaces selected repositories when their activity is relevant enough to track.';const reasons={AI:`AI is changing quickly, and this update from ${companies} could influence the tools developers use next.`,LLMs:'Model changes can affect capabilities, cost, speed and how AI applications are built.',Programming:'Language and developer-tool changes can directly affect how software is built and maintained.','Cyber Security':'Security updates can affect real systems immediately, especially when vulnerabilities or patches are involved.',Cloud:'Cloud platform changes can introduce new capabilities, infrastructure choices and development workflows.','Open Source':'Open-source releases can become useful building blocks for future projects and developer workflows.',Research:'Research developments can become the foundation for future products, models and engineering techniques.',Hardware:'Hardware changes can affect performance, AI workloads, local development and computing costs.'};return reasons[category]||'This is a technology change worth tracking because it may affect future products, tools or developer workflows.'}
function buildTakeaway(item){if(item.content_type==='podcast')return'You do not need to watch the whole episode first — start with these JARVIS-picked moments.';if(item.content_type==='github')return'JARVIS selected this repository because the owner and project activity meet its curated high-signal criteria.';const t={AI:'Track this one — it could shape what developers build with AI next.',LLMs:'Watch the model capability, speed and cost changes before choosing your next stack.',Programming:'A small developer-tool change can become a big workflow change over time.','Cyber Security':'If you use the affected technology, check for patches and security guidance.',Cloud:'Keep an eye on how this changes cloud architecture, tooling or costs.','Open Source':'Worth watching if you build with open-source software or developer infrastructure.',Research:'Today’s research can become tomorrow’s developer or product capability.',Hardware:'Hardware changes can directly influence performance and the cost of computing.'};return t[item.category]||'Worth tracking for its potential impact on future technology and developer workflows.'}
function cleanText(value){return String(value||'').replace(/<[^>]*>/g,' ').replace(/&nbsp;/gi,' ').replace(/\s+/g,' ').trim()}
function formatDate(value){const d=new Date(value);return Number.isNaN(d.getTime())?'Today':d.toLocaleDateString([],{month:'short',day:'numeric',year:'numeric'})}
function escapeHtml(value){return String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function setLoading(loading){refresh.disabled=loading;refresh.innerHTML=loading?'Updating...':'<span>↻</span> Refresh'}

search.addEventListener('input',render);category.addEventListener('change',render);importance.addEventListener('change',loadNews);refresh.addEventListener('click',loadNews);
const brief=document.getElementById('brief'),briefPanel=document.getElementById('briefPanel'),closeBrief=document.getElementById('closeBrief'),upload=document.getElementById('upload'),screenshotInput=document.getElementById('screenshotInput'),screenshotPanel=document.getElementById('screenshotPanel'),screenshotPreview=document.getElementById('screenshotPreview'),clearScreenshot=document.getElementById('clearScreenshot');
const analyzeScreenshot=document.createElement('button');analyzeScreenshot.type='button';analyzeScreenshot.textContent='Analyze';analyzeScreenshot.style.display='none';screenshotPanel.appendChild(analyzeScreenshot);const screenshotResult=document.createElement('div');screenshotResult.className='screenshot-result';screenshotResult.style.gridColumn='1 / -1';screenshotPanel.appendChild(screenshotResult);
brief.addEventListener('click',async()=>{brief.disabled=true;brief.innerHTML='<span>✦</span> Building...';try{const r=await fetch(`/brief?minimum_importance=${importance.value}`);if(!r.ok)throw 0;const d=await r.json();document.getElementById('briefHeadline').textContent=d.headline;document.getElementById('briefSummary').textContent=d.summary;document.getElementById('briefStories').innerHTML=d.stories.map(s=>`<article class="brief-story"><strong>${escapeHtml(s.title)}</strong><span>${escapeHtml(s.category)}</span><p>${escapeHtml(s.opportunity)}</p></article>`).join('');briefPanel.hidden=false;briefPanel.scrollIntoView({behavior:'smooth',block:'center'})}catch{document.getElementById('briefHeadline').textContent='Brief unavailable';document.getElementById('briefSummary').textContent='JARVIS could not build the intelligence brief right now.';briefPanel.hidden=false}finally{brief.disabled=false;brief.innerHTML='<span>✦</span> Brief'}});
closeBrief.addEventListener('click',()=>{briefPanel.hidden=true});upload.addEventListener('click',()=>screenshotInput.click());screenshotInput.addEventListener('change',e=>{const file=e.target.files?.[0];if(!file||!file.type.startsWith('image/'))return;screenshotPreview.src=URL.createObjectURL(file);screenshotPanel.hidden=false;analyzeScreenshot.style.display='inline-block';screenshotResult.innerHTML='<span>Screenshot ready. Press Analyze to send it through JARVIS.</span>';screenshotPanel.scrollIntoView({behavior:'smooth',block:'center'})});
analyzeScreenshot.addEventListener('click',async()=>{const file=screenshotInput.files?.[0];if(!file)return;analyzeScreenshot.disabled=true;analyzeScreenshot.textContent='Analyzing...';screenshotResult.textContent='JARVIS is extracting text and checking the signal feed...';try{const body=new FormData();body.append('file',file);const r=await fetch(`/screenshot/analyze?minimum_importance=${importance.value}`,{method:'POST',body});const d=await r.json();if(!r.ok||d.error)throw new Error(d.error||'Analysis failed');const matches=d.matches||[];screenshotResult.innerHTML=`<strong>${d.verified?'✓ Signal verified':'○ No matching signal yet'}</strong><p>Record: ${escapeHtml(d.record_id)} · OCR: ${escapeHtml(d.ocr?.status||'unknown')}</p>${d.extracted_text?`<pre>${escapeHtml(d.extracted_text.slice(0,1800))}</pre>`:'<p>No readable text was extracted. Install Tesseract on the host to enable OCR.</p>'}${matches.length?`<div>${matches.map(m=>`<article class="brief-story"><strong>${escapeHtml(m.title)}</strong><span>${escapeHtml(m.category)} · ${m.importance}/10</span><p>Matched: ${escapeHtml(m.matched_terms.join(', '))}</p></article>`).join('')}</div>`:''}`}catch(error){screenshotResult.textContent=error.message||'Screenshot analysis failed.'}finally{analyzeScreenshot.disabled=false;analyzeScreenshot.textContent='Analyze'}});
clearScreenshot.addEventListener('click',()=>{screenshotPreview.removeAttribute('src');screenshotInput.value='';screenshotResult.innerHTML='';analyzeScreenshot.style.display='none';screenshotPanel.hidden=true});
(async()=>{await loadCategories();await loadNews()})();
